# Dont' use nvm for now

# export NVM_DIR="$HOME/.nvm"
#
# nvm() {
#   unset -f nvm node npm npx
#   [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
#   [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
#   nvm "$@"
# }
#
# node() { nvm use --silent default; node "$@"; } # Automatically use default node version
# npm() { nvm use --silent default; npm "$@"; }
# npx() { nvm use --silent default; npx "$@"; }
#
